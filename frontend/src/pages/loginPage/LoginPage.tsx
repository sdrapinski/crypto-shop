import React from "react";
import LoginForm from "../../components/loginForm/LoginForm";

const LoginPage = () => {
  return (
    <div className="login">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
