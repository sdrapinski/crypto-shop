import React from "react";
import LoginForm from "../../components/Forms/LoginForm";

const LoginPage = () => {
  return (
    <div className="login">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
