import { NativeModules } from 'react-native';

type CBWrapperType = {
  multiply(a: number, b: number): Promise<number>;
};

const { CBWrapper } = NativeModules;

export default CBWrapper as CBWrapperType;
